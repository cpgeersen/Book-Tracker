# 2.9 - OpenLibrary API

### Main Objectives:
For this task, we will be creating the base GET functions to pull information from the OpenLibrary API.

- [ ] 2.9.1 - Pull all required book data for an ISBN search.
- [ ] 2.9.2 - Pull n-amount of book results based on book title (Pulled to separate task)
- [ ] 2.9.3 - Pull n-amount of book results based on author name

Data that must be pulled (if available) for a book result:
Note: If deferred do not worry about implementing yet.
 - Title
 - Publish Year
 - Summary
 - Author(s)
 - Publisher Name
 - ISBN
 - Works/OLID for Book, Author, and Publisher
 - Cover Image (deferred)
 - Genre(s) (deferred)

Note: OL = OpenLibrary
      OLID = OpenLibrary ID

Note: Right now as long as the data returned fulfills the needed book data as JSON, it is complete. Later we will
have the mediator transform the JSON to our variable naming and drop any unnecessary information before sending it to
the frontend or committing it to the database. 

Note: For now we are not pulling cover images yet, will be added later.

### Pseudocode

#### 2.9.1 - Pull all required book data for an ISBN search.
This is the simplest API function and most basic. We will be querying the OpenLibrary API based on an ISBN and will get
a JSON response with all the needed data.

```
get_book_data_from_isbn_OL(isbn): -> JSON
    
    # First call if the ISBN is already in database
    isbn_present["is_isbn_present"] = is_isbn_in_book_table(isbn) -> JSON (Unpack to get bool)
    
    IF bool(isbn_present):
        return read_full_book_record(isbn)
    ELSE:
        # Send GET request with ISBN to OL API
        # Step 1: GET information from ISBN
        # Step 2: If ISBN not present:
        #         return "ISBN not present in OpenLibrary, please use another ISBN or search via Title"
        # Step 3: Using the information pulled from ISBN search, pull information additional data using Author and 
        #         Publisher OLID
        # Step 4: Using received information, return JSON (including OLIDs, will be used later)

```

#### 2.9.3 - Pull n-amount of book results based on author name
This function will more likely than not return multiple books based on an author name.
For example, an author can write multiple books and if the name is vague could refer to different authors.
To alleviate some of the issues, users searching will be required to give a first name and a last name.
Though this solution does create the issue for authors like J.R.R. Tolkien who goes by J.R.R rather than a first name.
For this we will treat "J.R.R" as the first name and Tolkien as the last name.

```
get_books_from_author_OL(first_name, last_name)
    
    author_OLID = author_OLID_in_database(first_name, last_name) # New Function
    
    IF author_OLID:
        # Use the OLID in API request rather than first_name and last_name
    ELSE:
        # GET request using the first_name and last_name
        # Receive OLID for author
        # There could be multiple author OLID matches if a common first_name and last_name.
        # Just return result matches as JSON

```


























