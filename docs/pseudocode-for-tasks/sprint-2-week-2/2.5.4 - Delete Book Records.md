# 2.5.4 Delete Book Records

Note: Pseudocode and variable/function names are only for guidance. The only things that are a must 
are the inputs and outputs of the functions.

These functions will need to either use existing, reworked, or new query functions to construct.
The following functions are needed:
- delete_from_book_table(isbn) -> bool
- delete_from_book_author_table(isbn) -> bool
- delete_from_book_genre_table(isbn) -> bool
- delete_from_tag_table(isbn) -> bool

```
delete_book_by_isbn(isbn) -> bool
    
    # Delete Book Table Entry
    delete_from_book_table(isbn) -> bool
    
    # Delete BookAuthor Table Entry
    delete_from_book_author_table(isbn) -> bool
    
    # Delete BookGenre Table Entry
    delete_from_book_genre_table(isbn) -> bool
    
    # Delete Tag Table Entry
    delete_from_tag_table(isbn) -> bool
    
    # Delete BookNote and Note Table Entries
    # Will be implemented later

    # All others will not be deleted since other entries may depend on the records.
    
```