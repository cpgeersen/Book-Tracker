# 3.12 - Finalize Note Backend

### Main Objective:
Using the preexisting functions from `CRUD-Note.py` rework them so that we can finalize the backend functionality for
notes.

### Group Decision:
Should it be binary data stored in the table that we decode and encode, or we store them as raw txt files in the
data directory with paths to the files being stored in the Notes Table.

Why no just store note text directly in table?

#### NOTE: !!! Keep in mind most of the functionality for notes is complete, this is just to formalize the functions !!!


### 3.12.1 - Create Note
#### JSON Input:
```json
{
  "ISBN": "1234567890123",
  "Note_Content": "Note is here!" 
}
```

#### Pseudocode:
```
def create_note(json):
    isbn = json['ISBN']
    note_content = json['Note_Content']
    
    # Using an available Note_ID use the note_content
    # and create a new record in the Notes table
    
    # Then using the Note_ID and ISBN add a record
    # to the BookNotes briding table
```
#### JSON Output:
Returns the newly created Note_ID and Note_Content
```json
{
  "Note_ID": "11",
  "Note_Content": "Note is here!"
}
```

### 3.12.2 - Read Note
#### JSON Input:
```json
{
  "ISBN": "1234567890123"
}
```

#### Pseudocode:
```
def read_note(json):
    isbn = json['ISBN']
    
    # Using the ISBN get all note_ids in the bridging table
    
    # Get all note_content for all note_ids
```
#### JSON Output:
`Key = Note_ID`

`Value = Note_Content`
```json
{
  "2": "Some note.",
  "11": "Note is here!"
}
```

### 3.12.3 - Update Note
#### JSON Input:
```json
{
  "Note_ID": "11",
  "Note_Content": "New Note is here!" 
}
```

#### Pseudocode:
```
def update_note(json):
    note_id = json['Note_ID']
    note_content = json['Note_Content']
    
    # Using the Note_ID UPDATE the Note_Content
```
#### Output:
```
# Just return success or failure
```

### 3.12.4 - Delete Note
#### JSON Input:
```json
{
  "Note_ID": "11"
}
```

#### Pseudocode:
```
def delete_note(json)
    note_id = json['Note_ID']
    
    # Using the note_id delete the record from Note Table and BookNotes Table
```
#### Output:
```
# Just return success or failure
```