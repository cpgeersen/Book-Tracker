# 4.1 - User Settings

### Main Objective:
Create the JSON file that will be used to create the user settings and load them into memory.
Include functions to edit the file.

#### JSON Schema:

````JSON
    {
  "user_name": "",
  "theme": ""
}
````

#### JSON Functions

##### 4.1.1 - Create JSON user file
```
def create_user_json():
    with open() as out_json
        # write to json file

```

##### 4.1.2 - Edit JSON user file
```
def edit_user_json(user_json_file, json_from_frontend, 'edit_type'):
    if edit_type == 'user_name':
        # edit user name using json_from_frontend
        # and save to user_json_file
    elif edit_type == 'theme':
        # edit theme using json_from_frontend
        # and save to user_json_file
```

##### 4.1.3 - Read JSON user file
```
def read_user_json(user_json_file)
    # loads user settings into memory
```