# Task 4.19 - Cache OpenLibrary Search

### Main Objective:
Create a cacheing system in JSON for when a user searches OpenLibrary to save
previous searches.

### JSON Mock Data:
Note: All JSON is truncated, but the general gist of the schema is included.
#### JSON Schema:
```JSON
    {"Search_Query": "SOME_USER_SEARCH",
     "Results": {"Result_1": {"ISBN": "1234567890", "Title": "TRUNCATED_OTHER_BOOK_DATA"},
                 "Result_2": {"ISBN": "0987654321", "Title":  "OTHER_BOOK"}}
}
```
#### ISBN Search Example:
```JSON
    {"Search_Query": "1234567890",
     "Results": {"Result_1": {"ISBN": "1234567890", "Title": "TRUNCATED_OTHER_BOOK_DATA"}}
  
}
```
#### Author Search Example:
```JSON
    {"Search_Query": "Tolkien",
     "Results": {"Result_1": {"ISBN": "0618343997", "Title": "The Lord of the Rings", "MORE": "DATA_TRUNCATED"},
                 "Result_2": {"ISBN": "0261102427", "Title":  "The Silmarillion", "MORE": "DATA_TRUNCATED"}}
  
}
```
#### Title Search Example:
```JSON
    {"Search_Query": "Ring",
     "Results": {"Result_1": {"ISBN": "0618343997", "Title": "The Lord of the Rings", "MORE": "DATA_TRUNCATED"},
                 "Result_2": {"ISBN": "1590302486", "Title":  "The Book of Five Rings", "MORE": "DATA_TRUNCATED"}}
  
}
```

### Task 4.19.1 - JSON Cache
```
def cache(search_json):
    search_query = search_json['Search_Query']
    search_results = search_json['Results']

    if cache not present:
        json_cache = create_cache()
    else:
        # If the cache already exists
        json_cache = open(json_file_path)
    
    if search_json['Search_Query'] in json_cache:
        # Returns a previous search results if in cache
        return json_cache[search_json['Search_Query']]
    else:
        # add a search and search results to cache
        # Do not worry about intergration, use mock data with the given schema
    
        # Add search_json['Search_Query'] value as key to json_cache
        # and the value as the results from search_json['Results']
        json_cache[search_json['Search_Query']] = search_json['Results']
        save(json_cache)
        
        # Return the results for the given user search
        return json_cache[search_json['Search_Query']]

def create_cache():
    # Create empty json file if missing
    # Path: app/static/ol_json_cache.json
    return open(json_file_path)
```

### Task 4.19.2 - Purge JSON Cache
```
def purge_cache():
    if cache does not exist:
        # When the cache does not exist, 
        # just create an empty json file
        create_cache()
        return Success
    else:
        # Cache is present, does not matter if anything in it
        json_cache = open(json_file_path)
        json_cache.clear()
        save(json_cache)
        return Success
```




















