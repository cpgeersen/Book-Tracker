[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "book-tracker"
version = "0.1.0"

[tool.setuptools]
py-modules = ["run"]

[tool.setuptools.packages.find]
include = ["app*"]
