from flask import Flask, request
from UserManager import UserManager
from userProfileRefactored import update_User_Profile

app = Flask(__name__)

@app.route("/submit_Button", methods=["POST"])
def submit_button():
    username = request.form.get("usr_Name")
    mission = request.form.get("miss_Stmt")
    theme_switch = request.form.get("theme_switch")

    # Convert checkbox to 0 or 1
    theme_value = 1 if theme_switch == "on" else 0
    # I am not sure why the data dictionary isn't built and fed to the function.

    # {Alternative may be to use the Jinja Placeholders. 
    # Build the user profile dictionary}
    
    data = {
        "username": username,
        "mission_statement": mission,
        "theme": theme_value
    }
    
    # Call your existing function
    update_User_Profile(data)

    return data