def test_basic_multiplication():
    """
        Makes sure that the current pytest environment is currently working
        with a basic math assertion.

        :return: boolean
    """
    assert 2 * 2 == 4